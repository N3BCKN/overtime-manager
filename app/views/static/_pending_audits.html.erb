<% pending_audit_confirmations.each do |pac|%>
	<%= link_to "I confirm that I dit not perform any overtime for the week of: #{pac.start_date}", root_path, :class => "btn btn-primary btn-block btn-lg", data: {confirm: "are you sure?"} %>
<% end %>