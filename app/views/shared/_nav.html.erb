<div class="logo">
	<h1>Overtime tracker</h1>
</div>

<% if current_user %>
<ul class="custom-nav nav nav-tabs">
	<li class="nav-item <%= active?(root_path) %>">
		<%= link_to "", root_path, :class => "nav-link glyphicon glyphicon-home" %>
	</li>
	<li class="nav-item <%= active?(posts_path) %>">
		<%= link_to "Time Entries", posts_path, :class => "nav-link" %>
	</li>
		<li class="nav-item <%= active?(new_post_path)  %> ">
		<%= link_to "Entries", new_post_path, :class => "nav-link", :id =>"new_post_from_nav" %>
	</li>

	<% if policy(AuditLog).index? %>
		<%= render 'shared/audit_log_tab' %>
	<% end %> 

	 <li class="dropdown pull-right">
			 <a href="#" data-toggle="dropdown" class="dropdown-toggle">Options<strong class="caret"></strong></a>
			<ul class="dropdown-menu">
				
				<%= render 'shared/admin_dropdown' if admin? %>

				<li>
					<%= link_to "Logout", destroy_user_session_path, method: :delete %>
				</li>
			</ul>
		</li>
</ul>
<% end %>